{
  "name": "personal-blog",
  "version": "1.0.0",
  "description": "Personal Blog with Node.js backend and editorial frontend",
  "scripts": {
    "install": "cd backend && npm install",
    "dev": "cd backend && npm run dev",
    "start": "cd backend && npm start"
  }
}
